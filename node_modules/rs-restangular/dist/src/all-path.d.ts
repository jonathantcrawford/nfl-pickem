import { PathInterface } from './path.interface';
export declare class RestangularAllPath implements PathInterface {
    endpoint: string;
    constructor(endpoint: string);
    path(): Array<string>;
}
