import { PathInterface } from './path.interface';
export declare class RestangularOnePath implements PathInterface {
    endpoint: string;
    primaryKey: string;
    constructor(endpoint: string, primaryKey: string | number);
    path(): Array<string>;
}
